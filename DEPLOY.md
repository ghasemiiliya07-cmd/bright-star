# Bright Star
`node server.js` سپس `http://localhost:3000`
برای اجرای کامل سفارش و پرداخت آزمایشی، هاست Node.js لازم است. فایل render.yaml آماده است.
Netlify فقط نسخه استاتیک public را منتشر می‌کند و API server.js را اجرا نمی‌کند.
درگاه فعلی آزمایشی است و برای درگاه واقعی باید API درگاه و Merchant ID خودتان را متصل کنید.
